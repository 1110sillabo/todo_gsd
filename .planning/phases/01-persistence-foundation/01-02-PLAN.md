---
phase: 01-persistence-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/persistence/persistence_manager.gd, tests/unit/test_persistence_manager.gd]
autonomous: true
requirements: [PERSIST-02]
must_haves:
  truths:
    - "PersistenceManager can save a TaskResource as a .tres file"
    - "PersistenceManager can load a TaskResource from 'user://tasks/'"
    - "PersistenceManager can list all .tres files in 'user://tasks/'"
  artifacts:
    - path: "src/persistence/persistence_manager.gd"
      provides: "Singleton Manager for file operations"
    - path: "tests/unit/test_persistence_manager.gd"
      provides: "GdUnit4 test suite for persistence"
  key_links:
    - from: "src/persistence/persistence_manager.gd"
      to: "src/resources/task_resource.gd"
      via: "ResourceSaver/ResourceLoader"
---

<objective>
Implement the `PersistenceManager` singleton to handle saving and loading of `.tres` files.
Purpose: Provide a robust layer for managing file updates and listings for Task resources.
Output: `PersistenceManager` script and integration tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-persistence-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PersistenceManager singleton</name>
  <files>src/persistence/persistence_manager.gd</files>
  <behavior>
    - save_task(task: TaskResource) -> Error
    - load_task(filename: String) -> TaskResource
    - list_tasks() -> Array[String]
    - Path constant: 'user://tasks/'
  </behavior>
  <action>
    Implement PersistenceManager. 
    Manage directory creation if non-existent.
    Use `ResourceSaver` and `ResourceLoader`.
    Adhere to 3-Box architecture (persistence logic isolated).
  </action>
  <verify>
    <automated># Run unit tests
    godot --headless --path . -s addons/gdUnit4/bin/GdUnit4Cli.gd --test-dir tests/unit/test_persistence_manager.gd</automated>
  </verify>
  <done>PersistenceManager implemented and handles basic CRUD of .tres files.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests: Save and Load cycle</name>
  <files>tests/unit/test_persistence_manager.gd</files>
  <action>
    Implement test suite for PersistenceManager.
    Test saving a real TaskResource instance then loading it back.
    Verify all fields match after serialization/deserialization cycle.
  </action>
  <verify>
    <automated># Run persistence integration tests
    godot --headless --path . -s addons/gdUnit4/bin/GdUnit4Cli.gd --test-dir tests/unit/test_persistence_manager.gd</automated>
  </verify>
  <done>Save/Load cycle verified with full data integrity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File Access | PersistenceManager writes directly to 'user://'. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | Persistence | mitigate | Path traversal checks when loading files from disk. |
</threat_model>

<verification>
Ensure multiple tasks can be saved, listed, and individually loaded without error.
</verification>

<success_criteria>
- Task can be saved to 'user://tasks/'.
- Task can be loaded from 'user://tasks/'.
- Task listing functionality works.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-foundation/01-02-SUMMARY.md`
</output>
