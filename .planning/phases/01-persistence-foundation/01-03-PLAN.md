---
phase: 01-persistence-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/resources/task_resource.gd
  - src/resources/note_resource.gd
  - src/persistence/persistence_manager.gd
  - tests/unit/test_task_resource.gd
autonomous: true
requirements: [R10]
must_haves:
  truths:
    - "TaskResource has no graph_position field"
    - "NoteResource exists with title, content, created_at"
    - "PersistenceManager has no duplicate load_task function"
    - "PersistenceManager can save/load/list/delete notes"
    - "GdUnit4 tests parse without indentation errors"
  artifacts:
    - path: "src/resources/note_resource.gd"
      provides: "NoteResource data model"
      contains: "class_name NoteResource"
    - path: "src/persistence/persistence_manager.gd"
      provides: "Task and Note CRUD"
      contains: "func save_note"
  key_links:
    - from: "src/persistence/persistence_manager.gd"
      to: "src/resources/note_resource.gd"
      via: "save_note / load_note methods"
      pattern: "NoteResource"
---

<objective>
Patch the Phase 1 data layer to match the new mobile-first vision.
Remove the GraphEdit-specific `graph_position` field, introduce `NoteResource`,
fix the duplicate `load_task` parser error, and add note persistence methods.

Purpose: Align the data layer with the new roadmap before any UI work begins.
Output: Clean, bug-free data layer supporting both Tasks and Notes.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/resources/task_resource.gd
@src/resources/note_resource.gd
@src/persistence/persistence_manager.gd
@tests/unit/test_task_resource.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch TaskResource and create NoteResource</name>
  <files>src/resources/task_resource.gd, src/resources/note_resource.gd</files>
  <action>
    Remove `graph_position: Vector2` export from TaskResource — it belonged to the
    discarded GraphEdit design and is no longer relevant.
    Create NoteResource extending Resource with: `title: String`, `content: String`,
    `created_at: int` (auto-set in `_init` via `Time.get_unix_time_from_system()`).
  </action>
  <verify>grep -n "graph_position" src/resources/task_resource.gd | wc -l  # must be 0
grep -n "class_name NoteResource" src/resources/note_resource.gd  # must match</verify>
  <done>TaskResource has no graph_position. NoteResource file exists with correct class_name.</done>
</task>

<task type="auto">
  <name>Task 2: Fix PersistenceManager and add Note methods</name>
  <files>src/persistence/persistence_manager.gd</files>
  <action>
    Remove the first (empty/duplicate) `load_task` function definition — GDScript
    will refuse to parse a file with two functions of the same name.
    Add NOTES_PATH constant (`user://notes/`). Add four note methods following the
    exact same pattern as the task equivalents: `save_note`, `load_note`,
    `list_notes`, `delete_note`.
  </action>
  <verify>grep -c "func load_task" src/persistence/persistence_manager.gd  # must be 1
grep -n "func save_note\|func load_note\|func list_notes\|func delete_note" src/persistence/persistence_manager.gd  # must show 4 lines</verify>
  <done>No duplicate functions. Four note CRUD methods present.</done>
</task>

<task type="auto">
  <name>Task 3: Fix test indentation</name>
  <files>tests/unit/test_task_resource.gd</files>
  <action>
    Convert 4-space indentation to tabs throughout `test_task_resource.gd`.
    GDScript requires tabs — space indentation causes parse errors in headless runs.
  </action>
  <verify>cat -A tests/unit/test_task_resource.gd | grep "^\^I"  # tab-indented lines should appear
# No space-indented lines inside functions</verify>
  <done>Test file uses tabs. No parse errors when loaded by Godot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → FS | PersistenceManager writes user data to user:// directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | persistence_manager.gd | accept | Data is local, single-user app, no network exposure |
| T-01-03-02 | Information Disclosure | user://notes/ | accept | Device-local storage; OS-level protection applies |
</threat_model>

<verification>
grep -n "graph_position" src/resources/task_resource.gd  # should return nothing
grep -c "func load_task" src/persistence/persistence_manager.gd  # should be 1
grep -n "class_name NoteResource" src/resources/note_resource.gd  # should match
</verification>

<success_criteria>
- No graph_position anywhere in the data layer.
- NoteResource is a valid Godot Resource with title, content, created_at.
- PersistenceManager parses without errors and supports both Tasks and Notes.
- GdUnit4 test files use tab indentation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-foundation/01-03-SUMMARY.md`
</output>
