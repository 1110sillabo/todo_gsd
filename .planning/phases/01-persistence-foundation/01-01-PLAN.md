---
phase: 01-persistence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/resources/task_resource.gd, tests/unit/test_task_resource.gd]
autonomous: true
requirements: [PERSIST-01, MODEL-01]
must_haves:
  truths:
    - "TaskResource has all fields from specification"
    - "TaskResource can mark itself complete with signal emission"
    - "Resource handles serialization/deserialization via Godot's .tres format"
  artifacts:
    - path: "src/resources/task_resource.gd"
      provides: "Task data model"
    - path: "tests/unit/test_task_resource.gd"
      provides: "GdUnit4 test suite for TaskResource"
  key_links: []
---

<objective>
Define the `TaskResource` data model and establish the GdUnit4 testing framework.
Purpose: Create the core data structure and ensure it behaves correctly before building the persistence layer.
Output: `TaskResource` GDScript and its corresponding unit tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/TASK_RESOURCE_SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up GdUnit4 test scaffold</name>
  <files>tests/unit/test_task_resource.gd</files>
  <action>
    Create a GdUnit4 test script for TaskResource. 
    Ensure it inherits from `GdUnitTestSuite`.
    Define basic test structure for TaskResource properties and methods.
  </action>
  <verify>
    <automated># Try running GdUnit4 from CLI if available, else verify file structure
    ls tests/unit/test_task_resource.gd</automated>
  </verify>
  <done>Testing framework scaffold for TaskResource is ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TaskResource</name>
  <files>src/resources/task_resource.gd</files>
  <behavior>
    - title, description, creation_date, deadline, completion_date, rescheduled_count, tags, spatial_data.
    - Method: mark_complete() -> emits task_completed and sets completion_date.
    - Method: serialize_to_markdown() -> returns markdown string.
  </behavior>
  <action>
    Implement `TaskResource` as a global class inheriting from `Resource` (Godot 4.6).
    Register it with `class_name TaskResource`.
    Per D-01: Ensure high fidelity to the research spec.
  </action>
  <verify>
    <automated># Run GdUnit4 tests
    godot --headless --path . -s addons/gdUnit4/bin/GdUnit4Cli.gd --test-dir tests/unit/test_task_resource.gd</automated>
  </verify>
  <done>TaskResource implemented and passes unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Resource Serialization | Malicious .tres files could contain scripts (Godot security risk). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | TaskResource | mitigate | Ensure strict type hinting and utilize Godot's safe resource loading if applicable. |
</threat_model>

<verification>
Run `tests/unit/test_task_resource.gd` to ensure all fields and logic are correct.
</verification>

<success_criteria>
- TaskResource exists with all specified fields.
- Unit tests cover creation, completion, and basic serialization.
</success_criteria>

<output>
After completion, create `.planning/phases/01-persistence-foundation/01-01-SUMMARY.md`
</output>
