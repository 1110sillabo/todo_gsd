---
phase: 02-taskbox
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/ui/group_section.gd
  - src/ui/group_section.tscn
autonomous: true
requirements: [R01, R02, R09]
must_haves:
  truths:
    - "GroupSection shows a colored left-accent header with label, count, and chevron"
    - "Tapping the header toggles the task list visible/hidden"
    - "GroupSection can add TaskRow instances dynamically"
    - "GroupSection emits signals when a task is completed or deleted from within it"
  artifacts:
    - path: "src/ui/group_section.gd"
      provides: "Collapsible group container with accent header"
      contains: "extends VBoxContainer"
    - path: "src/ui/group_section.tscn"
      provides: "Scene for GroupSection"
      contains: "VBoxContainer"
  key_links:
    - from: "src/ui/group_section.gd"
      to: "src/ui/task_row.tscn"
      via: "preload + instantiate"
      pattern: "preload.*task_row\\.tscn"
---

<objective>
Create the GroupSection scene — a collapsible section with a colored accent header
(group name, count, chevron) and a dynamic list of TaskRow children.

Purpose: TaskListView (Plan 04) uses three GroupSection instances for Scadute/Da fare/Completate.
Output: group_section.tscn + group_section.gd with collapse toggle and task management API.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/ui/task_row.gd
@src/resources/task_resource.gd
@.planning/phases/02-taskbox/02-UI-SPEC.md
@.planning/phases/02-taskbox/02-RESEARCH.md
</context>

<interfaces>
<!-- TaskRow signals GroupSection must connect to -->
```gdscript
# From task_row.gd:
signal task_completed(task: TaskResource)
signal task_deleted(task: TaskResource)
```

<!-- GroupSection public API consumed by TaskListView (Plan 04) -->
```gdscript
# GroupSection must expose:
func setup(label: String, accent_color: Color, starts_expanded: bool) -> void
func add_task(task: TaskResource) -> void
func remove_task(task: TaskResource) -> void
func clear() -> void
func get_task_count() -> int

signal task_completed(task: TaskResource)   # bubbled up from TaskRow
signal task_deleted(task: TaskResource)     # bubbled up from TaskRow
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GroupSection scene (tscn)</name>
  <files>src/ui/group_section.tscn</files>
  <action>
    Create group_section.tscn with this node tree:
      GroupSection (VBoxContainer) — script: group_section.gd
        HeaderPanel (PanelContainer) — custom_minimum_size Vector2(0, 48)
          HeaderHBox (HBoxContainer)
            GroupLabel (Label) — text "Group"
            CountLabel (Label) — text "(0)"
            Spacer (Control, size_flags_horizontal = SIZE_EXPAND_FILL)
            ChevronLabel (Label) — text "▾"
        ItemsContainer (VBoxContainer) — visible = true

    HeaderPanel StyleBoxFlat: bg_color (0.12, 0.12, 0.14), border_width_left = 4.
    border_color set dynamically in setup().
    GroupLabel: font_size 14, semibold.
    CountLabel: font_size 13, color muted gray.
    ChevronLabel: font_size 14.
    All nodes inside HeaderHBox have size_flags_vertical = SIZE_SHRINK_CENTER.
  </action>
  <verify>
    grep -n "HeaderPanel\|ItemsContainer\|ChevronLabel" src/ui/group_section.tscn
  </verify>
  <done>group_section.tscn exists with correct node hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: Create GroupSection script with collapse and task API</name>
  <files>src/ui/group_section.gd</files>
  <action>
    Create group_section.gd extending VBoxContainer:

    const TASK_ROW_SCENE = preload("res://src/ui/task_row.tscn")

    Signals:
      signal task_completed(task: TaskResource)
      signal task_deleted(task: TaskResource)

    Vars:
      var _expanded: bool = true
      var _accent_color: Color = Color.WHITE
      var _tasks: Array[TaskResource] = []

    setup(label, accent_color, starts_expanded):
      $HeaderPanel/HeaderHBox/GroupLabel.text = label
      _accent_color = accent_color
      _expanded = starts_expanded
      $ItemsContainer.visible = starts_expanded
      _update_chevron()
      _apply_accent_style()

    _apply_accent_style():
      var style := StyleBoxFlat.new()
      style.bg_color = Color(0.12, 0.12, 0.14)
      style.border_color = _accent_color
      style.border_width_left = 4
      style.set_corner_radius_all(2)
      $HeaderPanel.add_theme_stylebox_override("panel", style)

    _update_chevron():
      $HeaderPanel/HeaderHBox/ChevronLabel.text = "▾" if _expanded else "▸"

    _update_count():
      $HeaderPanel/HeaderHBox/CountLabel.text = "(%d)" % _tasks.size()

    _on_header_pressed():
      _expanded = !_expanded
      $ItemsContainer.visible = _expanded
      _update_chevron()

    add_task(task):
      _tasks.append(task)
      var row: TaskRow = TASK_ROW_SCENE.instantiate()
      $ItemsContainer.add_child(row)
      row.task = task
      row.task_completed.connect(_on_task_completed)
      row.task_deleted.connect(_on_task_deleted)
      _update_count()

    remove_task(task):
      for child in $ItemsContainer.get_children():
        if child is TaskRow and child.task == task:
          child.queue_free()
          break
      _tasks.erase(task)
      _update_count()

    clear():
      for child in $ItemsContainer.get_children():
        child.queue_free()
      _tasks.clear()
      _update_count()

    get_task_count(): return _tasks.size()

    _on_task_completed(task): task_completed.emit(task)
    _on_task_deleted(task): task_deleted.emit(task)

    In _ready(): connect $HeaderPanel.gui_input (or make HeaderPanel a Button) to _on_header_pressed.
    Simplest: change HeaderPanel to a Button node instead of PanelContainer so pressed signal
    is built-in. If using PanelContainer, add InputEventMouseButton / InputEventScreenTouch
    detection in _gui_input on the panel.
  </action>
  <verify>
    grep -n "signal task_completed\|func setup\|func add_task\|func clear\|TASK_ROW_SCENE" src/ui/group_section.gd
  </verify>
  <done>GroupSection script has full public API: setup, add_task, remove_task, clear, get_task_count, and both signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → UI | GroupSection instantiates TaskRow scenes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | group_section.gd | accept | Local app; no unbounded input; task count bounded by disk space |
</threat_model>

<verification>
group_section.tscn references group_section.gd.
group_section.gd exposes setup(), add_task(), remove_task(), clear(), get_task_count().
group_section.gd preloads task_row.tscn.
</verification>

<success_criteria>
- GroupSection renders with colored left accent bar.
- Tap on header toggles ItemsContainer visibility and chevron direction.
- add_task() adds a TaskRow to ItemsContainer.
- task_completed and task_deleted signals bubble up from child TaskRows.
- Count badge updates when tasks are added or removed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-taskbox/02-03-SUMMARY.md`
</output>
