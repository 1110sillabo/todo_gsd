---
phase: 02-taskbox
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - src/ui/main_scene.gd
  - src/ui/main_scene.tscn
  - src/ui/task_list_view.gd
  - src/ui/task_list_view.tscn
autonomous: false
requirements: [R01, R02, R03, R04, R07, R09, R10]
must_haves:
  truths:
    - "App opens to a TabContainer with Tasks, Note, and Stats tabs"
    - "Tasks tab shows three GroupSections: Scadute (red), Da fare (blue), Completate (green)"
    - "Completate starts collapsed; Scadute and Da fare start expanded"
    - "Existing tasks load from PersistenceManager on ready and appear in the correct group"
    - "FAB button opens a new-task modal dialog"
    - "Confirming the modal creates a task, saves it, and adds it to Da fare"
    - "Swiping a task right marks it complete (completed_at set) and moves it to Completate"
    - "Swiping a task left deletes it from disk and removes the row"
  artifacts:
    - path: "src/ui/main_scene.tscn"
      provides: "Root scene with TabContainer"
      contains: "TabContainer"
    - path: "src/ui/task_list_view.tscn"
      provides: "Tasks tab content with GroupSections and FAB"
      contains: "ScrollContainer"
    - path: "src/ui/task_list_view.gd"
      provides: "Task loading, categorization, and FAB logic"
      contains: "func _load_tasks"
  key_links:
    - from: "src/ui/task_list_view.gd"
      to: "PersistenceManager"
      via: "autoload global"
      pattern: "PersistenceManager\\.list_tasks\\|save_task\\|delete_task"
    - from: "src/ui/task_list_view.gd"
      to: "src/ui/group_section.tscn"
      via: "preload + @onready refs"
      pattern: "GroupSection_Expired\\|GroupSection_Todo\\|GroupSection_Completed"
---

<objective>
Wire everything together: create the MainScene with TabContainer shell, the TaskListView
that loads/categorizes tasks into the three GroupSections, the FAB new-task modal, and
the complete/delete response handlers.

Purpose: This is the runnable app. After this plan the app opens, shows tasks, and supports
the core create/complete/delete flow.
Output: main_scene.tscn + task_list_view.tscn + task_list_view.gd — the full working UI.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/ui/group_section.gd
@src/ui/task_row.gd
@src/resources/task_resource.gd
@src/persistence/persistence_manager.gd
@.planning/phases/02-taskbox/02-UI-SPEC.md
@.planning/phases/02-taskbox/02-RESEARCH.md
</context>

<interfaces>
<!-- GroupSection API (from Plan 03) -->
```gdscript
func setup(label: String, accent_color: Color, starts_expanded: bool) -> void
func add_task(task: TaskResource) -> void
func remove_task(task: TaskResource) -> void
func clear() -> void
signal task_completed(task: TaskResource)
signal task_deleted(task: TaskResource)
```

<!-- PersistenceManager (autoload, from Plan 01) -->
```gdscript
PersistenceManager.list_tasks() -> Array[String]
PersistenceManager.load_task(filename: String) -> TaskResource
PersistenceManager.save_task(task: TaskResource) -> Error
PersistenceManager.delete_task(filename: String) -> Error
```

<!-- TaskResource (from Phase 1 patch) -->
```gdscript
@export var task_id: String
@export var title: String
@export var deadline: int     # 0 = no deadline
@export var completed_at: int # 0 = not complete
func mark_complete() -> void
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create MainScene with TabContainer and placeholder tabs</name>
  <files>src/ui/main_scene.tscn, src/ui/main_scene.gd</files>
  <action>
    Create main_scene.tscn as the application root scene. Node tree:
      MainScene (Control, anchors_preset PRESET_FULL_RECT) — script: main_scene.gd
        TabContainer (anchors_preset PRESET_FULL_RECT)
          TasksTab (Control, anchors_preset PRESET_FULL_RECT) — tab title "Da fare"
            [TaskListView scene instance — see Task 2]
          NotesTab (Control, anchors_preset PRESET_FULL_RECT) — tab title "Note"
            Label — text "In arrivo…", horizontal_alignment CENTER
          StatsTab (Control, anchors_preset PRESET_FULL_RECT) — tab title "Stats"
            Label — text "In arrivo…", horizontal_alignment CENTER

    In main_scene.gd (extends Control):
      @onready var tab_container: TabContainer = $TabContainer
      func _ready():
        tab_container.set_tab_disabled(2, true)  # Stats tab disabled

    Set project.godot application/run/main_scene to "res://src/ui/main_scene.tscn".

    Update project.godot config/name to "GSD Todo" (cleaner app name).
  </action>
  <verify>
    grep -n "main_scene" project.godot  # must show main scene path
    grep -n "TabContainer\|NotesTab\|StatsTab" src/ui/main_scene.tscn
  </verify>
  <done>MainScene runs, shows three tabs, Stats tab is disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskListView with GroupSections, FAB, and new-task modal</name>
  <files>src/ui/task_list_view.tscn, src/ui/task_list_view.gd</files>
  <action>
    Create task_list_view.tscn. Node tree:
      TaskListView (Control, anchors_preset PRESET_FULL_RECT) — script: task_list_view.gd
        ScrollContainer (anchors_preset PRESET_FULL_RECT)
          TaskListVBox (VBoxContainer, size_flags_horizontal SIZE_EXPAND_FILL)
            GroupSection_Expired (instance of group_section.tscn)
            GroupSection_Todo (instance of group_section.tscn)
            GroupSection_Completed (instance of group_section.tscn)
        FABButton (Button, layout_mode LAYOUT_MODE_ANCHORS)
          anchors: left=1, top=1, right=1, bottom=1
          offsets: left=-76, top=-76, right=-20, bottom=-20
          custom_minimum_size: Vector2(56, 56)
          text: "+"
          z_index: 1

    Create task_list_view.gd (extends Control):

    @onready var group_expired: GroupSection = $ScrollContainer/TaskListVBox/GroupSection_Expired
    @onready var group_todo: GroupSection = $ScrollContainer/TaskListVBox/GroupSection_Todo
    @onready var group_completed: GroupSection = $ScrollContainer/TaskListVBox/GroupSection_Completed
    @onready var fab: Button = $FABButton

    func _ready():
      group_expired.setup("Scadute", Color(0.85, 0.25, 0.18), true)
      group_todo.setup("Da fare", Color(0.22, 0.50, 0.90), true)
      group_completed.setup("Completate", Color(0.25, 0.72, 0.45), false)

      group_expired.task_completed.connect(_on_task_completed)
      group_expired.task_deleted.connect(_on_task_deleted)
      group_todo.task_completed.connect(_on_task_completed)
      group_todo.task_deleted.connect(_on_task_deleted)
      group_completed.task_completed.connect(_on_task_completed)
      group_completed.task_deleted.connect(_on_task_deleted)

      fab.pressed.connect(_on_fab_pressed)
      _load_tasks()

    func _load_tasks():
      group_expired.clear()
      group_todo.clear()
      group_completed.clear()
      var now := int(Time.get_unix_time_from_system())
      for filename in PersistenceManager.list_tasks():
        var task := PersistenceManager.load_task(filename)
        if task == null: continue
        if task.completed_at > 0:
          group_completed.add_task(task)
        elif task.deadline > 0 and task.deadline < now:
          group_expired.add_task(task)
        else:
          group_todo.add_task(task)

    func _on_fab_pressed():
      var dialog := AcceptDialog.new()
      dialog.title = "Nuovo task"
      var vbox := VBoxContainer.new()
      var line_edit := LineEdit.new()
      line_edit.placeholder_text = "Titolo del task…"
      vbox.add_child(line_edit)
      dialog.add_child(vbox)
      add_child(dialog)
      dialog.confirmed.connect(func(): _create_task(line_edit.text, dialog))
      dialog.canceled.connect(func(): dialog.queue_free())
      dialog.popup_centered(Vector2(320, 160))
      line_edit.grab_focus()

    func _create_task(title: String, dialog: AcceptDialog):
      dialog.queue_free()
      if title.strip_edges().is_empty(): return
      var task := TaskResource.new()
      task.title = title.strip_edges()
      PersistenceManager.save_task(task)
      group_todo.add_task(task)

    func _on_task_completed(task: TaskResource):
      task.mark_complete()
      PersistenceManager.save_task(task)
      # Remove from current group, add to completed
      group_expired.remove_task(task)
      group_todo.remove_task(task)
      group_completed.add_task(task)

    func _on_task_deleted(task: TaskResource):
      group_expired.remove_task(task)
      group_todo.remove_task(task)
      group_completed.remove_task(task)
      # Find filename by task_id and delete
      for filename in PersistenceManager.list_tasks():
        if filename.begins_with(task.task_id):
          PersistenceManager.delete_task(filename)
          break
  </action>
  <verify>
    grep -n "func _load_tasks\|func _on_fab_pressed\|func _on_task_completed\|func _on_task_deleted" src/ui/task_list_view.gd
    grep -n "GroupSection_Expired\|GroupSection_Todo\|GroupSection_Completed\|FABButton" src/ui/task_list_view.tscn
  </verify>
  <done>TaskListView loads tasks into correct groups, FAB opens modal, complete/delete work end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Task List UI in Godot editor</name>
  <action>Human verification — run the project and test the UI flows described below.</action>
  <files>N/A — verification only</files>
  <verify>Manual steps listed in how-to-verify below.</verify>
  <done>User types "approved" after verifying all flows work correctly.</done>
  <what-built>
    Complete Task List UI: tab bar, three collapsible grouped sections (Scadute/Da fare/Completate),
    FAB → new task modal, swipe to complete/delete. App launches from main_scene.tscn.
  </what-built>
  <how-to-verify>
    1. Open Godot editor and run the project (F5 or play button).
    2. Verify three tabs appear: "Da fare", "Note", "Stats" (Stats grayed out).
    3. Tap "+" FAB → modal opens with title field.
    4. Enter a task title → confirm → task appears in "Da fare" group.
    5. Restart the app → task still appears (persistence check).
    6. Swipe a task row right → task moves to "Completate" group.
    7. Swipe a task row left → task disappears.
    8. Tap "Completate" header → group collapses/expands.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → TaskResource | Task title entered in modal dialog |
| App → PersistenceManager → FS | Task CRUD through autoload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | task_list_view.gd | mitigate | Strip whitespace from title before save; empty title rejected |
| T-02-04-02 | Denial of Service | _load_tasks | accept | Local app; disk-bounded; no network surface |
| T-02-04-03 | Information Disclosure | user://tasks/ | accept | Device-local; OS-level protection |
</threat_model>

<verification>
- App runs from main_scene.tscn without errors.
- All three tabs visible; Stats disabled.
- Tasks load into correct groups on startup.
- FAB opens modal; confirmed task appears in Da fare.
- Swipe right → complete → row moves to Completate.
- Swipe left → delete → row removed.
- Restart → persisted tasks reload correctly.
</verification>

<success_criteria>
- MainScene launches with TabContainer showing Da fare / Note / Stats.
- TaskListView correctly categorizes tasks: expired → Scadute, no-deadline/future → Da fare, completed → Completate.
- Completate starts collapsed; others expanded.
- Full create/complete/delete flow works.
- Data persists across app restarts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-taskbox/02-04-SUMMARY.md`
</output>
