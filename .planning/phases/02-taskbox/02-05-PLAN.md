---
phase: 02-taskbox
plan: 05
type: tdd
wave: 4
depends_on: [02-04]
files_modified:
  - tests/unit/test_task_categorization.gd
  - tests/unit/test_task_list_view.gd
autonomous: true
requirements: [R01, R02, R04]
must_haves:
  truths:
    - "Task categorization logic is unit-tested: expired/todo/completed boundaries verified"
    - "UI integration: GroupSection add/remove/count behavior is tested"
  artifacts:
    - path: "tests/unit/test_task_categorization.gd"
      provides: "Unit tests for task grouping logic"
      contains: "extends GdUnitTestSuite"
    - path: "tests/unit/test_task_list_view.gd"
      provides: "GdUnit4 SceneRunner tests for TaskListView"
      contains: "extends GdUnitTestSuite"
  key_links:
    - from: "tests/unit/test_task_list_view.gd"
      to: "src/ui/task_list_view.tscn"
      via: "SceneRunner load"
      pattern: "task_list_view\\.tscn"
---

<objective>
Write GdUnit4 tests covering task categorization logic and TaskListView UI behavior.
Uses Red→Green→Refactor TDD cycle.

Purpose: Prevent regressions in the grouping logic and ensure UI interactions are
verifiable in headless mode.
Output: Two test files covering categorization and scene behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/resources/task_resource.gd
@src/ui/task_list_view.gd
@src/ui/task_list_view.tscn
@src/ui/group_section.gd
@addons/gdUnit4/GdUnitTestSuite.gd
</context>

<feature>
  <name>Task Categorization and TaskListView UI</name>
  <files>tests/unit/test_task_categorization.gd, tests/unit/test_task_list_view.gd</files>
  <behavior>
    Categorization (pure logic — no scene needed):
    - Task with completed_at > 0 → Completate group
    - Task with deadline < now and completed_at == 0 → Scadute group
    - Task with deadline == 0 → Da fare group
    - Task with deadline > now → Da fare group

    TaskListView UI (SceneRunner):
    - Scene loads without errors
    - GroupSection_Todo starts expanded (visible = true)
    - GroupSection_Completed starts collapsed (visible = false for items)
    - After _on_fab_pressed equivalent: task count in Todo increases by 1
  </behavior>
  <implementation>
    Use GdUnit4 SceneRunner for scene tests.
    For categorization, instantiate TaskResource directly and test the same boundary
    logic used in _load_tasks() — no scene required.
    Use tabs (not spaces) throughout. Extend GdUnitTestSuite.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing categorization tests then implement</name>
  <files>tests/unit/test_task_categorization.gd</files>
  <behavior>
    - test_completed_task_goes_to_completed: task.completed_at = 1 → category = "completed"
    - test_expired_task_goes_to_expired: task.deadline = past_timestamp → category = "expired"
    - test_no_deadline_task_goes_to_todo: task.deadline = 0 → category = "todo"
    - test_future_deadline_goes_to_todo: task.deadline = future_timestamp → category = "todo"
  </behavior>
  <action>
    RED: Write all four test functions. Each calls a helper func _categorize(task) that
    returns a String ("expired", "todo", "completed"). Run — all fail (function missing).

    GREEN: Add a static helper func _categorize(task: TaskResource) -> String inline in the
    test file OR as a static method on TaskListView (preferred — then test imports it).
    Run — all pass.

    Use tabs for indentation throughout. Extend GdUnitTestSuite.
  </action>
  <verify>
    grep -n "func test_" tests/unit/test_task_categorization.gd | wc -l  # must be 4
  </verify>
  <done>Four categorization tests pass in headless mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write TaskListView scene integration tests</name>
  <files>tests/unit/test_task_list_view.gd</files>
  <behavior>
    - test_scene_loads: SceneRunner loads task_list_view.tscn without error
    - test_completed_group_starts_collapsed: GroupSection_Completed/ItemsContainer visible = false
    - test_todo_group_starts_expanded: GroupSection_Todo/ItemsContainer visible = true
    - test_add_task_increases_todo_count: call add_task on group_todo, assert get_task_count() == 1
  </behavior>
  <action>
    RED: Write all four test functions using GdUnit4 SceneRunner API:
      var runner = scene_runner("res://src/ui/task_list_view.tscn")
      var view = runner.scene()
    Run — may fail if scene has errors.

    GREEN: Fix any scene loading issues found, run until all pass.
    Use tabs for indentation. Extend GdUnitTestSuite.
  </action>
  <verify>
    grep -n "func test_" tests/unit/test_task_list_view.gd | wc -l  # must be 4
  </verify>
  <done>Four TaskListView tests pass in headless mode or GdUnit4 editor runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Scene | SceneRunner loads production scenes in test context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | test files | accept | Tests run in isolated headless context; no production data affected |
</threat_model>

<verification>
godot --headless -s tests/unit/test_task_categorization.gd  # all 4 pass
godot --headless -s tests/unit/test_task_list_view.gd       # all 4 pass
</verification>

<success_criteria>
- 8 total tests pass (4 categorization + 4 UI).
- No space-indented lines in test files (tabs only).
- Tests run in under 60 seconds headless.
</success_criteria>

<output>
After completion, create `.planning/phases/02-taskbox/02-05-SUMMARY.md`
</output>
