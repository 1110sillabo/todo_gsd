---
phase: 02-taskbox
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/task_row.gd
  - src/ui/task_row.tscn
autonomous: true
requirements: [R01, R03, R09]
must_haves:
  truths:
    - "TaskRow displays a task title and deadline in a two-line layout"
    - "TaskRow emits a signal when swiped right (complete) or left (delete)"
    - "TaskRow touch target is at least 56px tall"
  artifacts:
    - path: "src/ui/task_row.gd"
      provides: "Reusable task row Control with swipe detection"
      contains: "extends PanelContainer"
    - path: "src/ui/task_row.tscn"
      provides: "Scene for TaskRow"
      contains: "PanelContainer"
  key_links:
    - from: "src/ui/task_row.gd"
      to: "src/resources/task_resource.gd"
      via: "task property setter"
      pattern: "var task: TaskResource"
---

<objective>
Create the reusable TaskRow scene — a single task item in the list. Shows title + deadline,
detects swipe gestures to complete or delete, emits signals upward.

Purpose: The atomic unit of the task list. GroupSection (Plan 03) will instance these.
Output: task_row.tscn + task_row.gd with full swipe detection and signal API.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/resources/task_resource.gd
@.planning/phases/02-taskbox/02-UI-SPEC.md
@.planning/phases/02-taskbox/02-RESEARCH.md
</context>

<interfaces>
<!-- TaskResource fields available to TaskRow -->
```gdscript
class_name TaskResource extends Resource
@export var task_id: String = ""
@export var title: String = ""
@export var description: String = ""
@export var deadline: int = 0       # Unix timestamp; 0 = no deadline
@export var completed_at: int = 0   # 0 = not complete
@export var reschedule_count: int = 0
@export var tags: Array[String] = []
```

<!-- Signal API TaskRow must expose (consumed by GroupSection in Plan 03) -->
```gdscript
signal task_completed(task: TaskResource)
signal task_deleted(task: TaskResource)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskRow scene (tscn)</name>
  <files>src/ui/task_row.tscn</files>
  <action>
    Create task_row.tscn with this node tree:
      TaskRow (PanelContainer) — script: task_row.gd
        MarginContainer — margins: left/right 12, top/bottom 10
          VBoxContainer
            TitleLabel (Label) — text placeholder "Task title"
            DeadlineLabel (Label) — text placeholder "Scadenza"

    PanelContainer custom_minimum_size = Vector2(0, 56).
    StyleBoxFlat on PanelContainer: bg_color dark (0.10, 0.10, 0.12), corner_radius_all 4.
    TitleLabel: theme_override font_sizes/font_size = 16, bold.
    DeadlineLabel: theme_override font_sizes/font_size = 13, color muted gray (0.55, 0.55, 0.60).
    Both Labels: size_flags_horizontal = SIZE_EXPAND_FILL.
  </action>
  <verify>
    grep -n "PanelContainer\|TitleLabel\|DeadlineLabel" src/ui/task_row.tscn
  </verify>
  <done>task_row.tscn exists with correct node hierarchy.</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskRow script with data binding and swipe detection</name>
  <files>src/ui/task_row.gd</files>
  <action>
    Create task_row.gd extending PanelContainer:

    Signals:
      signal task_completed(task: TaskResource)
      signal task_deleted(task: TaskResource)

    Properties:
      var task: TaskResource: set = set_task
      var _swipe_start: Vector2 = Vector2.ZERO
      const SWIPE_THRESHOLD := 80.0

    set_task(value):
      task = value
      $MarginContainer/VBoxContainer/TitleLabel.text = task.title
      if task.deadline > 0:
        var now := int(Time.get_unix_time_from_system())
        var dt := Time.get_datetime_dict_from_unix_time(task.deadline)
        $MarginContainer/VBoxContainer/DeadlineLabel.text = "%02d/%02d/%04d" % [dt.day, dt.month, dt.year]
        if task.deadline < now:
          $MarginContainer/VBoxContainer/DeadlineLabel.add_theme_color_override("font_color", Color(0.85, 0.25, 0.18))
      else:
        $MarginContainer/VBoxContainer/DeadlineLabel.visible = false

    _gui_input(event):
      if event is InputEventScreenTouch and event.pressed:
        _swipe_start = event.position
      elif event is InputEventScreenDrag and _swipe_start != Vector2.ZERO:
        var delta_x := event.position.x - _swipe_start.x
        var delta_y := event.position.y - _swipe_start.y
        if abs(delta_x) > SWIPE_THRESHOLD and abs(delta_x) > abs(delta_y) * 1.5:
          _swipe_start = Vector2.ZERO
          if delta_x > 0:
            task_completed.emit(task)
          else:
            task_deleted.emit(task)
  </action>
  <verify>
    grep -n "signal task_completed\|signal task_deleted\|_gui_input\|SWIPE_THRESHOLD" src/ui/task_row.gd
  </verify>
  <done>TaskRow script exists with property setter, swipe detection, and both signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Resource | TaskRow reads TaskResource fields directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | task_row.gd | accept | Read-only display; mutations go through PersistenceManager |
</threat_model>

<verification>
File src/ui/task_row.tscn exists and references task_row.gd.
File src/ui/task_row.gd has signal task_completed, signal task_deleted, set_task, _gui_input.
</verification>

<success_criteria>
- TaskRow scene loads without errors in Godot editor.
- set_task() populates labels correctly.
- Swipe right emits task_completed, swipe left emits task_deleted.
- Expired deadlines render in red.
- No-deadline tasks hide the deadline label.
- Touch target min 56px height.
</success_criteria>

<output>
After completion, create `.planning/phases/02-taskbox/02-02-SUMMARY.md`
</output>
