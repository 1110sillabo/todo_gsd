---
phase: 02-taskbox
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified: [tests/unit/test_task_box.gd]
autonomous: true
requirements: [TASKBOX-UI-03, TASKBOX-UI-04]
must_haves:
  truths:
    - "New tasks are programmatically added to GraphEdit"
    - "TaskNodes in GraphEdit can be programmatically moved"
    - "Updating TaskNode location results in correct data state"
  artifacts:
    - path: "tests/unit/test_task_box.gd"
      provides: "GdUnit4 test file for TaskBox UI"
      contains: "extends GdUnitTestSuite"
  key_links:
    - from: "tests/unit/test_task_box.gd"
      to: "src/ui/task_box.tscn"
      via: "auto_free(load().instantiate())"
      pattern: "load\\(.*task_box\\.tscn.*\\)"
---

<objective>
Use GdUnit4 SceneRunner to verify the interactive behavior of Phase 2's components. This ensures reliable UI/UX and proper sync with the persistence layer.

Purpose: Provide automated UI verification.
Output: GdUnit4 test suite for TaskBox.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/task_box.tscn
@src/ui/task_box.gd
</context>

<feature>
  <name>TaskBox UI Interaction Integration</name>
  <files>[src/ui/task_box.gd, tests/unit/test_task_box.gd]</files>
  <behavior>
    - Load TaskBox scene: Should load and instance TaskNodes for all .tres tasks.
    - Click "Add Task" button: Verification that a new TaskNode is appended to GraphEdit.
    - Simulated Drag: Move a TaskNode and assert its TaskResource's `graph_position` matches the new offset.
    - Ensure graph zoom/pan settings are enabled for mobile (GraphEdit properties).
  </behavior>
  <implementation>
    Use `SceneRunner` from GdUnit4 to simulate inputs and monitor tree state.
  </implementation>
</feature>

<verification>
Automated check: `./addons/gdUnit4/bin/gdunit4_cli.sh -s tests/unit/test_task_box.gd`
</verification>

<success_criteria>
- Tests pass in headless mode or editor runner.
- New tasks are verified in the scene tree.
- Data synchronization is verified across the UI and Resource.
</success_criteria>

<output>
After completion, create `.planning/phases/02-taskbox/02-02-SUMMARY.md`
</output>
